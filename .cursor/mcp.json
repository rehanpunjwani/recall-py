{
  "mcpServers": {
    "tokenguard": {
      "command": "bash",
      "args": ["scripts/mcp-stdio.sh"],
      "cwd": "${workspaceFolder}",
      "env": {
        "TOKENGUARD_WORKSPACE": "${workspaceFolder}"
      }
    }
  }
}
